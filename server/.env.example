# PostgreSQL Database Configuration
DB_USER=dmw_app
DB_HOST=localhost
DB_NAME=dmw_procurement
DB_PASSWORD=SecurePass2026#DMW
DB_PORT=5432

# Server Configuration
PORT=3000
HOST=0.0.0.0

# JWT Secret (change this in production!)
JWT_SECRET=dmw_procurement_secret_key_2026_change_in_production

# JWT Expiration (e.g., 8h for 8 hours, 1d for 1 day)
JWT_EXPIRES_IN=8h
